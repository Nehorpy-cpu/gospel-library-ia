apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gospel-services
spec:
  selector:
    matchExpressions:
      - key: app
        operator: In
        values: ["rag-api", "scraper-api", "qdrant"]
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
