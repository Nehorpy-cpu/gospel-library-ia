This folder is reserved for migrations generated by Prisma:

```bash
npm run migrate:dev -- --name init_enterprise
```

PostgreSQL-specific add-ons that Prisma cannot express live in:

```txt
packages/database/sql/post_migrate
```
